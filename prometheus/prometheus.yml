global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Scrape GenLayer node metrics
  - job_name: "genlayer-node"
    static_configs:
      - targets:
          # Ajuste aqui caso o node esteja em outro host/IP.
          # Se o node estiver no MESMO host que esse stack Docker,
          # e você usar Docker Desktop / similar, host.docker.internal funciona.
          - "host.docker.internal:9153"
    metrics_path: /metrics